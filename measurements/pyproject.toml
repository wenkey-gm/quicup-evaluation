[tool.poetry]
name = "measurements"
version = "0.1.0"
description = ""
authors = ["Muni Venkatesh Ganji"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^2.4.3"
matplotlib = "^3.10.8"
pandas = "^3.0.1"
seaborn = "^0.13.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
